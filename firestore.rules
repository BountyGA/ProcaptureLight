rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Matches any document in the products collection
    match /products/{productId} {
      allow read, write: if true;
    }
    
    // Default fallback rules
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
